import React from 'react';
import ReactDOM from 'react-dom';
import { Card, Tag, Space } from 'antd';

const Health=()=>{
    return(
<Card width="100%" height="100%">
<div>This is Health section</div>
</Card>
    );
};

export default Health;
